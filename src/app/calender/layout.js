export const metadata = {
  title: 'Anime Release Calendar',
  description: 'Track upcoming anime episodes and their release schedules',
};

export default function CalendarLayout({ children }) {
  return children;
} 