export const metadata = {
  title: 'Random Anime Generator',
  description: 'Discover new anime with our random anime generator',
};

export default function RandomAnimeLayout({ children }) {
  return children;
} 